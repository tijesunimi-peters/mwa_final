import { AuthLinkDirective } from './auth-link.directive';

describe('AuthLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthLinkDirective();
    expect(directive).toBeTruthy();
  });
});
